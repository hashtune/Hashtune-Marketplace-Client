overwrite: true
schema: "http://localhost:5000/graphql"
documents: "graphql/**/**/*.ts"
generates:
  graphql/generated/apolloComponents.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
